//! IPC payload structs shared between `abo-core` and the `src-tauri` command
//! layer, each deriving `serde::Serialize`, `serde::Deserialize`, and
//! `specta::Type`.
//!
//! Stub for v0.1.0 spine, Phase 1. Phase 4 defines the v0.1.0 payload
//! structs here (for example `ScanSummary`, `EntryRow` or an equivalent
//! shape) (AC-4).
