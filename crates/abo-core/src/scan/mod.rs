//! Live tree scanner (F-101), file typing (F-103), and snapshot persistence
//! (F-105).
//!
//! Stub for v0.1.0 spine, Phase 1. Phase 3 implements `walk.rs` (the
//! `walkdir`-based traversal with extended-length `\\?\` semantics),
//! `typing.rs` (the extension-to-class table, including the FD-17 `video`
//! class), and `persist.rs` (the `scans` and `entries` write path)
//! (AC-9 through AC-13).
